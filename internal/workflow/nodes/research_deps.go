// Package nodes provides built-in workflow node types.
// Shared service dependencies are injected via workflow.NodeContext,
// passed by the engine during execution. No global state is used.
package nodes

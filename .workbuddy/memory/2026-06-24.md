# 2026-06-24 工作日志

## QuantFlow 项目评审（代码 + 金融双视角）
- 以代码专家 + 金融专家双视角对 QuantFlow Terminal 做全面评审
- 派 3 个 Explore agent 并行深审：后端 Go(internal/)、Python sidecar(python/)、前端(frontend/)
- 综合评分 7.4/10：架构设计 8.5、工程规范 8.5、测试覆盖 8.0、安全性 7.5、代码质量 7.5、可维护性 7.0、前端工程 7.0、金融专业性 6.5
- 亮点：分层清晰无循环依赖、Spec-Before-Code 工程纪律、672 测试、零 panic、Arrow IPC 零拷贝
- P0 致命问题（金融正确性）：T+1 多标的失效(engine_cn.go:121)、OMS FillOrder 裁剪不一致(oms.go:122-161)、CacheKey map 序列化非确定(cache.go:32)、涨跌停缺失
- P1：无 look-ahead/survivorship bias 防护、ML 无验证集(tree_engine.py:110)、横截面因子失效(factor/engine.py:44)、Calmar 量纲不匹配(risk.go:94)、前端 ref<Map> 响应性失效(data.ts:57)、IPC 未统一
- 产出：docs/QuantFlow_项目评审报告.html（含评分雷达图 + P0/P1/P2 分级 + 改进路线图）

## 编写修复 Spec
- 基于评审发现，逐条核对实际代码行号后编写修复 spec：docs/specs/2026-06-24-fix-review-correctness.md
- 核对代码后修正了两处初版过激判断：
  - T+1 锁定：P0→P1。日频下"读取在 bar 开头、清空在 bar 末尾"巧合正确，仅分钟频/日内频失效(engine_cn.go:121)
  - CacheKey map 序列化：P0→P1。map %v 顺序随机致 cache miss(性能)，不会错误命中(cache.go:33)
- 确认仍为 P0：OMS FillOrder 裁剪顺序(oms.go:122-161)、涨跌停缺失(engine_cn.go:12 仅注释)、横截面因子失效(factor/engine.py:44)、ML 无验证集(tree_engine.py:110)
- spec 含 4 P0 + 7 P1 + 6 P2，每项附根因/修复/影响文件/验收标准，含 models 表 schema 迁移、三阶段实施顺序
- 遵循项目 CLAUDE.md 的 Spec-Before-Code 规范

## 编写 Phase A 实施计划
- 基于修复 spec 编写 Phase A (P0 正确性) 实施计划：docs/superpowers/plans/2026-06-24-fix-review-phase-a.md
- Phase A 含 4 个独立 Task，每个遵循 TDD（写失败测试→实现→通过→commit），含完整代码：
  - A1: OMS FillOrder 裁剪顺序（先裁剪后更新订单账本，新增 oms_test.go）
  - A2: 涨跌停限制（新建 price_limit.go + price_limit_test.go + engine_cn 集成测试，按 symbol 前缀识别板块）
  - A3: 横截面因子分发（engine.py 按 is_cross_sectional 分发完整 panel vs 逐 symbol）
  - A4: ML 验证集划分（train_test_split shuffle=False，返回 val_* 指标）
- 核对代码行号准确：oms.go:122-176、engine_cn.go:51-130、engine.py:20-127、tree_engine.py:97-153
- 每个 Task 含 commit message 模板，遵循项目 [Engine]/[Python] scope 约定
- 计划末尾含 Phase A 统一验收 + CHANGELOG + 版本号同步指引，并指向 Phase B

## 执行 Phase A（subagent-driven-development）
- 分支 fix-review-phase-a，基线 e5ea350，6 commits，最终 HEAD f974460
- 用 subagent-driven-development skill 执行：每 Task 派 implementer → 生成 review-package → 派 task reviewer → 修复 findings → 标记完成
- A1 OMS 裁剪 (f97cc11)：一次通过，无 issues
- A2 涨跌停 (41dfdc7→77cdb26)：review 发现止损路径绕过 CanSell（Critical），自己直接修复（fix subagent 空返回）+ 补 3 个集成测试（跌停卖出、创业板 ±20%、跌停止损）
- A3 横截面因子 (961b795)：review Approved，实现者合理修正了 brief 测试数据 bug（同增长率致 zscore 不稳定），RPC 集成测试 brief-mandated 简化
- A4 ML 验证集 (3aee9a2)：review Approved，classification 小样本 warning 非对称是 brief-mandated
- 最终 whole-branch review (reasoning model)：Ready to merge
- 最终修复 (f974460)：BSE 注释 + CHANGELOG 记录 4 项 spec 偏差延后 Phase B
- 延后 Phase B：ST ±5%（需 Config override）、ML SQLite 模型注册表、分钟频 prevClose、横截面 RPC 集成测试、DRY 重构
- 工作流教训：fix subagent 偶发空返回，需 fallback 到自己直接修复；task-brief 脚本不支持 "Task A1" 命名（只认 "Task 1"），用 awk 手动提取

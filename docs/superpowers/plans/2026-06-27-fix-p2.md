# P2 边界优化实施计划

> **For agentic workers:** Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 修复 11 个 P2 边界/规范/可维护性问题

**Architecture:** 11 个独立任务，无依赖关系，可并行执行。分 Go(5) + Python(4) + 前端(2) 三组。

**Spec:** docs/specs/fix-p2-issues.md

**Tech Stack:** Go 1.22, Vue 3 + TypeScript, Python 3.12

## Global Constraints

- Go: `go test` 不引入新失败
- 前端: `vite build` 不新增编译错误
- Python: import 测试通过
- 每个任务 5-15 分钟

---

### Task 1: 方差样本估计 + VaR 符号

**Files:** `internal/backtest/metrics.go:58`, `internal/portfolio/risk.go:69,100`

- [ ] **Step 1: metrics.go — 改用样本方差**

```go
// metrics.go:58 — 替换
if nDays > 1 {
    annualVol = math.Sqrt(variance / float64(nDays-1)) * math.Sqrt(252)
} else {
    annualVol = 0
}
```

- [ ] **Step 2: risk.go — 同理**

```go
// risk.go:69 — 替换
if nDays > 1 {
    annualVol = math.Sqrt(variance / float64(nDays-1)) * math.Sqrt(252)
}
```

- [ ] **Step 3: risk.go — VaR 报正数**

```go
// risk.go:100 — 替换
VaR95: math.Abs(var95) * totalValue,
```

- [ ] **Step 4: 测试**

```bash
go test ./internal/backtest/ -v -timeout 30s && go test ./internal/portfolio/ -v -timeout 30s
```

---

### Task 2: ST 股 ±5% 涨跌停

**Files:** `internal/backtest/price_limit.go:10-11`

- [ ] **Step 1: 实现 ST 检测**

```go
// price_limit.go — PriceLimitFor 函数增加 ST 判断
func PriceLimitFor(symbol string) PriceLimitRule {
    // ST / *ST stocks: ±5%
    if strings.Contains(symbol, "ST") || strings.Contains(symbol, "*ST") {
        return PriceLimitRule{Ratio: 0.05, Name: "ST (±5%)"}
    }
    // ChiNext (300/301) and STAR (688): ±20%
    if strings.HasPrefix(symbol, "300") || strings.HasPrefix(symbol, "301") || strings.HasPrefix(symbol, "688") {
        return PriceLimitRule{Ratio: 0.20, Name: "ChiNext/STAR (±20%)"}
    }
    // Main board: ±10%
    return PriceLimitRule{Ratio: 0.10, Name: "Main Board (±10%)"}
}
```

- [ ] **Step 2: 确认 strings import 存在**

- [ ] **Step 3: 测试**

```bash
go test ./internal/backtest/ -run TestPriceLimitFor -v -timeout 30s
```

---

### Task 3: 订单 ID 完整 UUID

**Files:** `internal/trading/oms.go:110, 214`

- [ ] **Step 1: 改用 12 字符 UUID 截断**

```go
// oms.go:110 — 替换
ID: uuid.New().String()[:12],  // was [:8]

// oms.go:214 — 替换
ID: uuid.New().String()[:12],  // was [:8]
```

- [ ] **Step 2: 测试**

```bash
go test ./internal/trading/ -v -timeout 30s
```

---

### Task 4: 印花税率按日期可配

**Files:** `internal/backtest/engine_cn.go:60-89`

- [ ] **Step 1: CNEngine 增加税率字段**

```go
// engine_cn.go — CNEngine struct 增加
stampDutyRate float64 // 0.0005 default, configurable by date
```

- [ ] **Step 2: stampDuty 改为方法**

```go
// engine_cn.go:87-89 — 替换
func (e *CNEngine) stampDuty(tradeValue float64) float64 {
    return tradeValue * e.stampDutyRate
}

// NewCNEngine 中默认:
stampDutyRate: 0.0005,
```

- [ ] **Step 3: 调用点更新**

```go
// engine_cn.go 中所有 stampDuty(...) 调用改为 e.stampDuty(...)
```

- [ ] **Step 4: 测试**

```bash
go test ./internal/backtest/ -v -timeout 30s
```

---

### Task 5: ECharts 按需导入（CandlestickPanel）

**Files:** `frontend/src/terminal/panels/CandlestickPanel.vue:3-14`

CandlestickPanel 已经正确按需导入。本任务只需要移除 `import * as echarts`。

- [ ] **Step 1: 移除 import * as echarts**

```typescript
// CandlestickPanel.vue — 删除 line 8: import * as echarts from 'echarts'
// 保留已有的按需导入
```

- [ ] **Step 2: 检查是否依赖 echarts 全局**

```typescript
// 如果代码中有 `echarts.xxx` 引用，改为局部 register
// 当前已用 use([...]) 注册，无需 echarts 全局
```

- [ ] **Step 3: 验证构建**

```bash
cd frontend && npx vite build 2>&1 | tail -3
```

---

### Task 6: tsconfig 严格模式

**Files:** `frontend/tsconfig.json`

- [ ] **Step 1: 开启 strict**

```json
{
    "compilerOptions": {
        "strict": true,
        "noImplicitAny": false,
        "skipLibCheck": true
    }
}
```

`noImplicitAny: false` 避免破坏现有代码的 `any` 类型（71 处 `window as any`）。

- [ ] **Step 2: 验证构建**

```bash
cd frontend && npx vue-tsc --noEmit 2>&1 | head -20
```

如果 type errors 太多，回退 `strict` 但开启 `strictNullChecks` + `strictFunctionTypes` 两个子选项。

---

### Task 7: RLPredict 调用实际模型

**Files:** `python/src/ml/engine.py:208-220`

- [ ] **Step 1: 实现 RLPredict**

```python
# engine.py:208-220 — 替换 RLPredict 实现
async def RLPredict(self, request, context):
    """Predict action using trained RL model."""
    try:
        model_id = request.model_id
        if not model_id or model_id not in self.rl_models:
            context.set_code(grpc.StatusCode.NOT_FOUND)
            context.set_details(f"Model '{model_id}' not found")
            return ml_pb2.RLPredictResponse(action=1, confidence=0.0)  # hold
        
        model = self.rl_models[model_id]
        obs = np.array(request.observation).reshape(1, -1).astype(np.float32)
        action, _ = model.predict(obs, deterministic=True)
        return ml_pb2.RLPredictResponse(
            action=int(action[0]),
            confidence=float(abs(action[0])) if hasattr(action, '__len__') else 0.8
        )
    except Exception as e:
        context.set_code(grpc.StatusCode.INTERNAL)
        context.set_details(str(e))
        return ml_pb2.RLPredictResponse(action=1, confidence=0.0)  # fallback hold
```

---

### Task 8: DQN sharpe 除零保护

**Files:** `python/src/ml/rl/algorithms/dqn.py:94`

- [ ] **Step 1: 加保护**

```python
# dqn.py — 在 sharpe 计算处加保护
returns = pd.Series(equity_curve).pct_change().dropna()
if len(returns) < 2:
    return {"sharpe": 0.0, "max_drawdown": 0.0, "total_return": 0.0}
std = returns.std()
if std < 1e-10:
    return {"sharpe": 0.0, "max_drawdown": 0.0, "total_return": 0.0}
sharpe = returns.mean() / std * np.sqrt(252)
```

---

### Task 9: Anthropic finish_reason 映射

**Files:** `python/src/llm/anthropic_provider.py:157`

- [ ] **Step 1: 映射 finish_reason**

```python
# anthropic_provider.py:157 — 在 process_response 中
finish_reason = response.stop_reason or ""
# Map Anthropic stop reasons to OpenAI-compatible values
reason_map = {
    "end_turn": "stop",
    "max_tokens": "length",
    "stop_sequence": "stop",
    "tool_use": "tool_calls",
}
normalized_reason = reason_map.get(finish_reason, finish_reason)
```

---

### Task 10: 加密永续合约 stub

**Files:** Create `internal/market/adapters/binance_futures.go`

- [ ] **Step 1: 新建 binance_futures.go**

```go
package adapters

import (
    "encoding/json"
    "fmt"
    "net/http"
    "quantflow/internal/trading"
    "time"
)

const binanceFuturesBaseURL = "https://fapi.binance.com/fapi/v1"

type BinanceFuturesAdapter struct {
    client *http.Client
}

func NewBinanceFuturesAdapter() *BinanceFuturesAdapter {
    return &BinanceFuturesAdapter{client: &http.Client{Timeout: 10 * time.Second}}
}

func (b *BinanceFuturesAdapter) Name() string { return "binance_futures" }

func (b *BinanceFuturesAdapter) FetchOHLCV(symbol string, period string, limit int) ([]trading.OHLCVBar, error) {
    // USDT-M perpetual futures klines
    url := fmt.Sprintf("%s/klines?symbol=%s&interval=%s&limit=%d", binanceFuturesBaseURL, symbol, period, limit)
    resp, err := b.client.Get(url)
    if err != nil {
        return nil, fmt.Errorf("binance_futures: %w", err)
    }
    defer resp.Body.Close()
    // ... parse kline response ...
    return nil, fmt.Errorf("binance_futures: not yet implemented")
}

func (b *BinanceFuturesAdapter) FetchQuote(symbol string) (*trading.Quote, error) {
    url := fmt.Sprintf("%s/ticker/price?symbol=%s", binanceFuturesBaseURL, symbol)
    resp, err := b.client.Get(url)
    if err != nil {
        return nil, fmt.Errorf("binance_futures: %w", err)
    }
    defer resp.Body.Close()
    var result struct{ Symbol string `json:"symbol"`; Price string `json:"price"` }
    if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
        return nil, err
    }
    price, _ := parseFloat(result.Price)
    return &trading.Quote{Symbol: symbol, Price: price, Name: symbol}, nil
}
```

---

### Task 11: 全量验证

- [ ] **Step 1: Go 全量测试**

```bash
go test ./internal/... -timeout 60s 2>&1 | grep -E "(FAIL|ok|---)"
```

- [ ] **Step 2: 前端构建**

```bash
cd frontend && npx vite build 2>&1 | tail -3
```

- [ ] **Step 3: Python 导入**

```bash
cd python && python -c "from src.ml.engine import MLService; from src.llm.anthropic_provider import AnthropicProvider; print('OK')"
```

---

## 执行摘要

| Task | 描述 | 预计 |
|------|------|:--:|
| 1 | 方差 N-1 + VaR 正数 | 5m |
| 2 | ST 股 ±5% | 5m |
| 3 | 订单 ID 12字符 | 2m |
| 4 | 印花税率可配 | 10m |
| 5 | ECharts 去全量导入 | 5m |
| 6 | tsconfig strict | 10m |
| 7 | RLPredict 调模型 | 10m |
| 8 | DQN 除零保护 | 5m |
| 9 | Anthropic finish_reason | 5m |
| 10 | 加密合约 stub | 10m |
| 11 | 全量验证 | 5m |

**总计**: ~70m，可并行 3 组执行
